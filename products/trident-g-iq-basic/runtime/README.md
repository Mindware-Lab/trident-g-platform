# Basic App Runtime

This folder is for extracted runtime logic reused by `trident-g-iq-basic`.

Planned subfolders:

- `tests/`
- `zone/`
- `capacity/`

The rule is:

- reuse logic and data contracts from the legacy product
- do not reuse legacy UI markup or styling
