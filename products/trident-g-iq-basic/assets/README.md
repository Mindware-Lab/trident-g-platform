# Basic App Assets

This folder is reserved for runtime assets used by `trident-g-iq-basic`.

Planned subfolders:

- `tests/`
- `capacity/`

Only copy true reusable gameplay or assessment assets here. Do not copy legacy UI assets unless they are needed by the new shell.
