# Analytics

Telemetry contracts and interpretation notes (repo-safe).

Include:
- event schema (fields, types)
- metrics dictionary (what each metric means)
- export format examples

Do not store raw user/client data here.
